# Logical AND guard that should use optional chaining
id: prefer-optional-chain
language: typescript
severity: warning
message: "Use optional chaining `?.` instead of `&&` guard for property access."
rule:
  pattern: "$OBJ && $OBJ.$PROP"
